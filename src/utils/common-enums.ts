/**
 * Created by zack on 18/5/17.
 */
export enum LoadingStatus {
  none,
  loading,
  succeeded,
  failed,
}

export enum CheckboxCheckedState {
  none,
  indeterminate,
  all,
}

export enum SelectionState {
  selected,
  unselected,
  disabled,
}
